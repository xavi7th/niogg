---
phase: 03-custom-thumbnail-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/PublicPage/database/migrations/xxxx_add_custom_thumbnail_url_to_videos_table.php
  - Modules/PublicPage/app/Models/Video.php
autonomous: true
must_haves:
  truths:
    - "Database has custom_thumbnail_url column"
    - "Video model serializes thumbnail_url with fallback chain (custom -> auto-generated -> placeholder)"
    - "Custom thumbnails include cache-busting query parameter"
  artifacts:
    - path: "Modules/PublicPage/database/migrations"
      provides: "Migration adding custom_thumbnail_url column"
      contains: "add_custom_thumbnail_url_to_videos_table"
    - path: "Modules/PublicPage/app/Models/Video.php"
      provides: "Video model with custom_thumbnail_url support"
      contains: "custom_thumbnail_url"
      exports: ["getThumbnailUrlAttribute"]
  key_links:
    - from: "Video.php"
      to: "videos table"
      via: "Eloquent model"
      pattern: "protected\\s+\\$fillable.*custom_thumbnail_url"
---

<objective>
Add database column and model accessor for custom thumbnail URLs with fallback chain

Purpose: Enable storage of custom uploaded thumbnails while maintaining existing auto-generated and placeholder fallbacks
Output: Migration file and updated Video model with proper accessor
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Context from prior phases
@.planning/phases/01-fix-thumbnail-display/plans/01-03-add-lazythumbnail-to-grid-views-SUMMARY.md
@.planning/phases/02-fix-video-playback/02-03-SUMMARY.md

# Phase-specific research (HIGH confidence)
@.planning/phases/03-custom-thumbnail-upload/03-RESEARCH.md

# Phase context (locked decisions)
@.planning/phases/03-custom-thumbnail-upload/03-CONTEXT.md

# Existing code to modify
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/app/Models/Video.php
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/app/Services/VideoThumbnailService.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for custom_thumbnail_url column</name>
  <files>Modules/PublicPage/database/migrations/xxxx_add_custom_thumbnail_url_to_videos_table.php</files>
  <action>
    Generate migration using: `vendor/bin/sail artisan make:migration add_custom_thumbnail_url_to_videos_table --module=PublicPage --no-interaction`

    Migration must:
    - Add `custom_thumbnail_url` column (string, nullable) after `thumbnail_url`
    - No default value needed
  </action>
  <verify>Migration file created in Modules/PublicPage/database/migrations/</verify>
  <done>Migration file exists with proper column definition</done>
</task>

<task type="auto">
  <name>Task 2: Run migration to add column</name>
  <files>database</files>
  <action>
    Run migration: `vendor/bin/sail artisan migrate --no-interaction`

    This adds the custom_thumbnail_url column to the videos table.
  </action>
  <verify>vendor/bin/sail artisan migrate:status shows migration as completed</verify>
  <done>custom_thumbnail_url column exists in videos table</done>
</task>

<task type="auto">
  <name>Task 3: Update Video model with custom_thumbnail_url support</name>
  <files>Modules/PublicPage/app/Models/Video.php</files>
  <action>
    Update Video model:

    1. Add `custom_thumbnail_url` to $fillable array

    2. Add `thumbnail_url` to $appends array (to serialize accessor to JSON)

    3. Create `getThumbnailUrlAttribute()` accessor with fallback chain:
       - Priority 1: Custom thumbnail (if custom_thumbnail_url is not empty)
       - Priority 2: Auto-generated thumbnail (if thumbnail_url is not empty)
       - Priority 3: Placeholder image at `/images/video-placeholder-default.jpg`

    4. Append cache-busting query parameter to dynamic thumbnails:
       - For custom: `?v={$this->updated_at->timestamp}`
       - For auto-generated: `?v={$this->updated_at->timestamp}`
       - Placeholder: No cache busting needed

    IMPORTANT: Strip existing query strings before appending new ones using strtok()

    Example:
    ```php
    $url = strtok($url, '?') . '?v=' . $this->updated_at->timestamp;
    ```

    Reference existing format_duration accessor naming pattern (snake_case for JSON serialization)
  </action>
  <verify>
    1. vendor/bin/sail bin pint Modules/PublicPage/app/Models/Video.php
    2. Verify accessor returns proper fallback chain with test: `php artisan tinker --execute="echo Modules\\PublicPage\\Models\\Video::first()?->thumbnail_url;"`
  </verify>
  <done>Video model has custom_thumbnail_url in fillable, thumbnail_url accessor returns proper fallback chain with cache busting</done>
</task>

</tasks>

<verification>
- Migration exists and has been run
- Video model includes custom_thumbnail_url in fillable array
- Accessor getThumbnailUrlAttribute() returns correct fallback: custom → auto-generated → placeholder
- Cache busting query parameter (?v={timestamp}) applied to custom and auto-generated URLs
- Placeholder URL is /images/video-placeholder-default.jpg (no cache busting)
</verification>

<success_criteria>
Admin can now store custom thumbnail URLs in database, and the Video model automatically returns the appropriate thumbnail with fallback chain and cache busting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-custom-thumbnail-upload/plans/03-01-SUMMARY.md`
</output>
