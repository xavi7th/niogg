---
phase: 03-custom-thumbnail-upload
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Modules/PublicPage/app/Http/Requests/Admin/VideoThumbnailRequest.php
  - Modules/PublicPage/app/Http/Controllers/Admin/AdminVideoController.php
  - Modules/PublicPage/app/Services/VideoThumbnailService.php
autonomous: true
must_haves:
  truths:
    - "Form request validates thumbnail file (image format, max 5MB)"
    - "AdminVideoController has POST endpoint for thumbnail upload"
    - "VideoThumbnailService stores custom thumbnails with UUID filenames"
    - "Old thumbnail file is deleted when replaced"
  artifacts:
    - path: "Modules/PublicPage/app/Http/Requests/Admin/VideoThumbnailRequest.php"
      provides: "Form request validation for thumbnail uploads"
      exports: ["rules()", "messages()"]
    - path: "Modules/PublicPage/app/Http/Controllers/Admin/AdminVideoController.php"
      provides: "Thumbnail upload endpoint"
      contains: "uploadThumbnail"
      exports: ["POST /admin/videos/{id}/thumbnail"]
    - path: "Modules/PublicPage/app/Services/VideoThumbnailService.php"
      provides: "Custom thumbnail storage logic"
      contains: "storeCustomThumbnail"
      exports: ["storeCustomThumbnail", "deleteCustomThumbnail"]
  key_links:
    - from: "AdminVideoController.php"
      to: "VideoThumbnailService.php"
      via: "Dependency injection"
      pattern: "VideoThumbnailService.*\\$thumbnailService"
    - from: "AdminVideoController.php"
      to: "videos table"
      via: "Eloquent model"
      pattern: "\\$video->update.*custom_thumbnail_url"
    - from: "AdminVideoController.php"
      to: "VideoThumbnailRequest"
      via: "Method parameter type-hint"
      pattern: "uploadThumbnail.*VideoThumbnailRequest"
    - from: "VideoThumbnailRequest"
      to: "uploaded file"
      via: "field name extraction"
      pattern: "\\$request->file\\('thumbnail'\\)"
---

<objective>
Create backend upload handling with validation, storage, and old file cleanup

Purpose: Handle custom thumbnail uploads securely with proper validation and storage management
Output: Form request, controller endpoint, and service methods for thumbnail handling
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase work (required for dependencies)
@.planning/phases/03-custom-thumbnail-upload/plans/03-01-SUMMARY.md

# Phase-specific research (HIGH confidence)
@.planning/phases/03-custom-thumbnail-upload/03-RESEARCH.md

# Phase context (locked decisions)
@.planning/phases/03-custom-thumbnail-upload/03-CONTEXT.md

# Existing code to extend
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/app/Http/Controllers/Admin/AdminVideoController.php
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/app/Services/VideoThumbnailService.php
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/app/Http/Requests/Admin/VideoFormRequest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoThumbnailRequest Form Request</name>
  <files>Modules/PublicPage/app/Http/Requests/Admin/VideoThumbnailRequest.php</files>
  <action>
    Generate form request: `vendor/bin/sail artisan make:request Modules\\\\PublicPage\\\\Http\\\\Requests\\\\Admin\\\\VideoThumbnailRequest --no-interaction`

    Implement rules() method:
    - `thumbnail` field is required
    - Must be an image (use 'image' rule - covers jpeg, png, bmp, gif, svg)
    - Maximum 5120 KB (5MB)

    Implement messages() method with user-friendly error messages:
    - thumbnail.required: "Please select an image file."
    - thumbnail.image: "The file must be an image."
    - thumbnail.max: "The image may not be larger than 5MB."

    Reference VideoFormRequest.php for existing pattern
  </action>
  <verify>vendor/bin/sail bin pint Modules/PublicPage/app/Http/Requests/Admin/VideoThumbnailRequest.php</verify>
  <done>VideoThumbnailRequest validates thumbnail file with proper rules and messages</done>
</task>

<task type="auto">
  <name>Task 2: Add custom thumbnail methods to VideoThumbnailService</name>
  <files>Modules/PublicPage/app/Services/VideoThumbnailService.php</files>
  <action>
    Add these methods to VideoThumbnailService class:

    1. `storeCustomThumbnail(UploadedFile $file, Video $video): string`
       - Generate UUID-based filename preserving original extension
       - Path: `videos/thumbnails/custom/{uuid}.{ext}`
       - Delete old custom thumbnail if exists (call deleteCustomThumbnail)
       - Store file using Storage::disk('public')
       - Return full URL to stored file (e.g., "https://app.local/storage/videos/thumbnails/custom/{uuid}.jpg")

    2. `deleteCustomThumbnail(?string $customThumbnailUrl): void`
       - Strip storage URL to get relative path
       - Delete file if exists using Storage::disk('public')->delete()
       - Handle null/empty input gracefully

    Use existing deleteThumbnails() method as pattern for path stripping
  </action>
  <verify>vendor/bin/sail bin pint Modules/PublicPage/app/Services/VideoThumbnailService.php</verify>
  <done>VideoThumbnailService has storeCustomThumbnail and deleteCustomThumbnail methods</done>
</task>

<task type="auto">
  <name>Task 3: Add thumbnail upload endpoint to AdminVideoController</name>
  <files>Modules/PublicPage/app/Http/Controllers/Admin/AdminVideoController.php</files>
  <action>
    Update AdminVideoController:

    1. Add VideoThumbnailService to constructor dependency injection

    2. Create `uploadThumbnail(VideoThumbnailRequest $request, Video $video)` method:
       - Extract file from request using `$request->file('thumbnail')` (matches 'thumbnail' field name in VideoThumbnailRequest rules)
       - Store custom thumbnail via `$this->thumbnailService->storeCustomThumbnail($file, $video)`
       - Update video model with new custom_thumbnail_url
       - Return JSON response with video object and 200 status
       - Handle exceptions with try-catch (return 500 error on failure)

    3. Register route in Modules/PublicPage/routes/web.php:
       - POST /admin/videos/{video}/thumbnail
       - Route name: admin.videos.thumbnail
       - Middleware: auth (same as other admin routes)

    Use existing reorder() method as pattern for JSON responses and error handling
  </action>
  <verify>
    1. vendor/bin/sail bin pint Modules/PublicPage/app/Http/Controllers/Admin/AdminVideoController.php
    2. vendor/bin/sail artisan route:list --name=admin.videos.thumbnail
  </verify>
  <done>POST /admin/videos/{video}/thumbnail endpoint exists and updates custom_thumbnail_url</done>
</task>

</tasks>

<verification>
- VideoThumbnailRequest validates image format and 5MB max size
- VideoThumbnailService has storeCustomThumbnail() and deleteCustomThumbnail() methods
- AdminVideoController has uploadThumbnail() method
- Route POST /admin/videos/{video}/thumbnail is registered
- Old thumbnail files are deleted when new ones are uploaded
- Thumbnail files are stored in videos/thumbnails/custom/ with UUID filenames
</verification>

<success_criteria>
Backend can receive thumbnail uploads, validate them, store with UUID filenames, and properly clean up old files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-custom-thumbnail-upload/plans/03-02-SUMMARY.md`
</output>
