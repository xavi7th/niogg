---
phase: 03-custom-thumbnail-upload
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - Modules/PublicPage/resources/js/Components/Admin/VideoEditModal.svelte
autonomous: true
must_haves:
  truths:
    - "Admin can select image file in dedicated upload section"
    - "Thumbnail preview appears after file selection"
    - "Upload shows loading state during upload"
    - "Validation errors display inline for invalid files"
    - "Modal refreshes with new thumbnail after successful upload"
  artifacts:
    - path: "Modules/PublicPage/resources/js/Components/Admin/VideoEditModal.svelte"
      provides: "Thumbnail upload UI in video edit modal"
      contains: "uploadThumbnail"
      exports: ["uploadThumbnail() function", "file input", "loading state"]
  key_links:
    - from: "VideoEditModal.svelte"
      to: "/admin/videos/{id}/thumbnail"
      via: "Inertia router.post() with forceFormData"
      pattern: "router\\.post.*thumbnail.*forceFormData"
    - from: "VideoEditModal.svelte"
      to: "Video model"
      via: "Inertia props"
      pattern: "video\\.custom_thumbnail_url|video\\.thumbnail_url"
---

<objective>
Add thumbnail upload UI to video edit modal with preview, loading states, and error handling

Purpose: Admin users can upload custom thumbnails through an intuitive interface
Output: Updated VideoEditModal with thumbnail upload section
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase work (required for dependencies)
@.planning/phases/03-custom-thumbnail-upload/plans/03-01-SUMMARY.md
@.planning/phases/03-custom-thumbnail-upload/plans/03-02-SUMMARY.md

# Phase-specific research (HIGH confidence)
@.planning/phases/03-custom-thumbnail-upload/03-RESEARCH.md

# Phase context (locked decisions)
@.planning/phases/03-custom-thumbnail-upload/03-CONTEXT.md

# Existing code to extend
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Components/Admin/VideoEditModal.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add thumbnail upload state and bindings to VideoEditModal</name>
  <files>Modules/PublicPage/resources/js/Components/Admin/VideoEditModal.svelte</files>
  <action>
    Add new state variables in <script> section:

    1. `let thumbnailFile = null;` - Selected file object
    2. `let thumbnailPreview = null;` - URL for preview
    3. `let uploadingThumbnail = false;` - Upload in progress flag
    4. `let thumbnailProgress = 0;` - Upload progress percentage
    5. `let thumbnailError = null;` - Validation/upload error message
    6. `let fileInput;` - Reference to file input element

    Create computed current thumbnail URL:
    ```javascript
    $: currentThumbnail = video.custom_thumbnail_url || video.thumbnail_url || '/images/video-placeholder-default.jpg';
    ```

    Import router from @inertiajs/svelte (already imported)
  </action>
  <verify>State variables added to component script section</verify>
  <done>Component has thumbnail upload state variables and computed URL</done>
</task>

<task type="auto">
  <name>Task 2: Add uploadThumbnail function</name>
  <files>Modules/PublicPage/resources/js/Components/Admin/VideoEditModal.svelte</files>
  <action>
    Add `uploadThumbnail()` function:

    1. Validate file is selected
    2. Create FormData and append thumbnail file
    3. Set uploadingThumbnail = true, thumbnailError = null
    4. Call router.post() to `/admin/videos/${video.id}/thumbnail`:
       - forceFormData: true
       - onProgress: update thumbnailProgress
       - onSuccess: refresh video data via router.reload(), reset state
       - onError: capture errors, display in thumbnailError, reset uploading flag
    5. Reset state on complete/error

    Use existing submit() function as pattern for error handling and state management
  </action>
  <verify>Function exists with proper FormData handling and router.post() call</verify>
  <done>uploadThumbnail function sends FormData to backend with progress tracking, modal refreshes with new thumbnail on success, errors display inline</done>
</task>

<task type="auto">
  <name>Task 3: Add thumbnail upload section to modal body</name>
  <files>Modules/PublicPage/resources/js/Components/Admin/VideoEditModal.svelte</files>
  <action>
    Add new section in modal body (before existing form fields or after description):

    **Section structure:**

    1. Section header: "Custom Thumbnail" label
    2. Current thumbnail preview:
       - Show image at currentThumbnail URL
       - Styling: rounded, max height 200px, object-cover
       - Border with brand color if custom thumbnail exists

    3. Upload controls (dedicated section, NOT inline on thumbnail):
       - File input (bind:this={fileInput}, hidden)
       - "Choose Image" button (clicks file input)
       - After selection: show file name and size details
       - During upload: replace entire section with loading spinner and "Uploading {thumbnailProgress}%"
       - After upload: show new preview

    4. Error display:
       - Show thumbnailError in red text if set
       - Display inline below upload controls

    5. Remove button: NO (per CONTEXT.md decision - thumbnails can only be replaced, not removed)

    **Styling:**
    - Use existing Tailwind classes from modal
    - Brand color: #ff7607
    - Match existing form field spacing
    - Preview image: max-height-48 object-cover rounded-lg border-2

    Reference existing input fields for styling patterns
  </action>
  <verify>npm run lint fixes any formatting issues</verify>
  <done>Modal has dedicated thumbnail upload section with preview, file input, loading state, and error display</done>
</task>

</tasks>

<verification>
- VideoEditModal has dedicated thumbnail upload section
- Clicking "Choose Image" opens file picker
- Selected file shows preview with filename/size
- Uploading shows loading state with progress percentage
- Successful upload refreshes modal with new thumbnail
- Validation errors (non-image, >5MB) display inline
- No remove button (thumbnails replace-only, per CONTEXT.md decision)
</verification>

<success_criteria>
Admin users can upload custom thumbnails through an intuitive interface with visual feedback and error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/03-custom-thumbnail-upload/plans/03-03-SUMMARY.md`
</output>
