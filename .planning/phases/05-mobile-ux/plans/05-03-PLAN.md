---
phase: 05-mobile-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/VideoPlayer.svelte
  - /Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/EventsFeaturedOnly.svelte
autonomous: false

must_haves:
  truths:
    - "VideoPlayer component has playsinline attribute for iOS inline playback"
    - "VideoPlayer component has muted attribute for autoplay policy compliance"
    - "EventsFeaturedOnly modal has playsinline and muted attributes"
    - "Mobile video modal has 44x44px close button for touch targets"
    - "Videos play correctly on iOS Safari (14+) and Chrome Android (10+)"
  artifacts:
    - path: "/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/VideoPlayer.svelte"
      provides: "Desktop timeline video player with mobile attributes"
      contains: "playsinline" and "muted"
    - path: "/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/EventsFeaturedOnly.svelte"
      provides: "Mobile featured view with video modal"
      contains: "playsinline" and "muted" and "modal-close"
  key_links:
    - from: "VideoPlayer video element"
      to: "iOS/Android native playback"
      via: "playsinline and muted attributes"
      pattern: "playsinline.*muted"
    - from: "EventsFeaturedOnly modal video"
      to: "Mobile inline playback"
      via: "playsinline attribute prevents iOS fullscreen"
      pattern: "video.*playsinline"
---

<objective>
Verify video components have mobile-compatible attributes (playsinline, muted) and test playback on actual mobile devices. This plan requires human verification on iOS Safari and Chrome Android.

Purpose: Ensure videos play correctly on mobile devices without forcing fullscreen
Output: Verified mobile playback working on iOS Safari and Chrome Android
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-mobile-ux/05-RESEARCH.md
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/VideoPlayer.svelte
@/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/EventsFeaturedOnly.svelte
@.planning/phases/02-fix-video-playback/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Verify VideoPlayer has mobile attributes</name>
  <files>/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/VideoPlayer.svelte</files>
  <action>
    Read VideoPlayer.svelte and verify video element attributes:

    1. Lines 53-66: Video element has required mobile attributes
    2. Line 59: `playsinline` attribute present (prevents iOS fullscreen)
    3. Line 60: `muted` attribute present (enables autoplay compliance)
    4. Line 56: `controls` attribute present (native touch controls)
    5. Line 58: `preload="metadata"` for efficient loading

    These attributes were added in Phase 2 (plan 02-03) per STATE.md decision.
  </action>
  <verify>VideoPlayer video element has playsinline, muted, controls, and preload attributes</verify>
  <done>VideoPlayer component has all required mobile attributes for cross-platform playback</done>
</task>

<task type="auto">
  <name>Verify EventsFeaturedOnly modal has mobile attributes</name>
  <files>/Users/leinad/Work/htdocs/asuke-niogg.org/Modules/PublicPage/resources/js/Pages/Components/EventsFeaturedOnly.svelte</files>
  <action>
    Read EventsFeaturedOnly.svelte and verify modal video attributes:

    1. Lines 150-164: Modal video element attributes
    2. Line 156: `muted` attribute present
    3. Line 157: `playsinline` attribute present
    4. Line 154: `controls` attribute present
    5. Line 155: `autoplay` attribute present (muted autoplay works on mobile)
    6. Line 158: `on:canplay={handleCanPlay}` for reliable autoplay
    7. Line 159: `on:error={handleVideoError}` for error handling
    8. Lines 390-404: Modal close button with `width: 44px` and `height: 44px` (meets WCAG AAA)

    Confirm mobile modal has proper attributes and touch targets.
  </action>
  <verify>Modal video has playsinline, muted, controls, autoplay; close button is 44x44px minimum</verify>
  <done>EventsFeaturedOnly modal has mobile-compatible video attributes and touch-optimized close button</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Video playback components with mobile attributes (playsinline, muted, controls)
    - VideoPlayer.svelte: Desktop timeline player
    - EventsFeaturedOnly.svelte: Mobile featured view with modal
  </what-built>
  <how-to-verify>
    Test on actual mobile devices (or Chrome DevTools device emulation):

    **iOS Safari (14+) testing:**
    1. Open /events/media-showcase on iOS device or simulator
    2. Verify mobile view shows featured videos only (EventsFeaturedOnly)
    3. Tap featured video to open modal
    4. Verify video plays inline (not fullscreen) with `playsinline`
    5. Verify muted autoplay works (video starts playing automatically)
    6. Tap unmute in native controls to enable audio
    7. Tap close button (44x44px) to dismiss modal

    **Chrome Android (10+) testing:**
    1. Open /events/media-showcase on Android device
    2. Verify mobile view shows featured videos only
    3. Tap featured video to open modal
    4. Verify video plays with `playsinline` and `muted`
    5. Verify native controls are touch-optimized
    6. Tap close button to dismiss modal

    **Desktop verification:**
    1. Resize browser to < 768px width
    2. Confirm EventTimeline disappears, EventsFeaturedOnly appears
    3. Test modal opens and plays video correctly

    **Expected results:**
    - Video doesn't force fullscreen on iOS
    - Autoplay works (muted by default)
    - Close button is tappable (44x44px minimum)
    - Native controls work on touch devices
  </how-to-verify>
  <resume-signal>
    After testing, type "approved" if mobile playback works correctly.
    If issues found, describe what failed (e.g., "video goes fullscreen on iOS", "autoplay doesn't work", "close button too small").
  </resume-signal>
</task>

</tasks>

<verification>
After completing all tasks:
- VideoPlayer has playsinline and muted attributes
- EventsFeaturedOnly modal has playsinline and muted attributes
- Modal close button is 44x44px (WCAG AAA compliant)
- Human testing confirms videos play correctly on iOS Safari and Chrome Android
- Autoplay works on mobile (muted)
- No fullscreen forcing on iOS
</verification>

<success_criteria>
Videos play correctly on mobile devices (iOS Safari 14+, Chrome Android 10+)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-ux/05-03-SUMMARY.md`
</output>
