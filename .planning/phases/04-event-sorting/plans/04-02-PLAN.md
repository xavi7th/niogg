---
phase: 04-event-sorting
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - Modules/PublicPage/app/Http/Controllers/EventsMediaShowcaseController.php
autonomous: true

must_haves:
  truths:
    - "Controller reads 'sort' query parameter from URL (?sort=newest|oldest)"
    - "Controller maps sort value to direction ('newest' -> 'desc', 'oldest' -> 'asc')"
    - "Controller passes direction to Event::ordered() scope"
    - "Default sort is 'newest' when parameter missing"
  artifacts:
    - path: "Modules/PublicPage/app/Http/Controllers/EventsMediaShowcaseController.php"
      provides: "Sort parameter handling in controller"
      contains: "\$request->input('sort'"
      min_lines: 10
  key_links:
    - from: "Modules/PublicPage/app/Http/Controllers/EventsMediaShowcaseController.php"
      to: "Modules/PublicPage/app/Models/Event.php"
      via: "Event::ordered(\$direction)"
      pattern: "ordered\\(\\\$?\\w+\\)"
    - from: "Browser URL"
      to: "EventsMediaShowcaseController"
      via: "Query parameter ?sort="
      pattern: "\\?sort=(newest|oldest)"
---

<objective>
Update EventsMediaShowcaseController to read sort query parameter and pass direction to Event::ordered().

Purpose: Server-side handler for URL-based sorting - converts 'newest'|'oldest' from URL to 'desc'|'asc' for model scope.
Output: Controller index() method with sort parameter handling.
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-sorting/04-CONTEXT.md
@.planning/phases/04-event-sorting/04-RESEARCH.md

# Current implementation
@Modules/PublicPage/app/Http/Controllers/EventsMediaShowcaseController.php
@.planning/phases/04-event-sorting/plans/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add sort parameter handling to controller index() method</name>
  <files>Modules/PublicPage/app/Http/Controllers/EventsMediaShowcaseController.php</files>
  <action>
    1. Import Request class at top of file (if not already imported):
       ```php
       use Illuminate\Http\Request;
       ```

    2. Update index() method signature to accept Request parameter:
       - Change: `public function index()`
       - To: `public function index(Request $request)`

    3. Add sort parameter reading and direction mapping after opening brace:
       ```php
       $sort = $request->input('sort', 'newest');
       $direction = $sort === 'newest' ? 'desc' : 'asc';
       ```

    4. Update Event query to pass direction to ordered():
       - Change: `->ordered()`
       - To: `->ordered($direction)`

    5. Optionally pass sort value to frontend (for active link styling):
       - Add to Inertia::render() array: `'sort' => $sort`
       - This will be available as $page.props.sort in Svelte

    DO NOT:
    - Modify the show() or eventVideos() methods
    - Change the published() scope call
    - Modify the videos relationship eager loading
    - Add any validation (sort values are whitelapped by ternary)
  </action>
  <verify>Visit URL in browser: /events?sort=newest and /events?sort=oldest, verify events load in correct order</verify>
  <done>Accessing /events?sort=newest shows events newest-first, /events?sort=oldest shows events oldest-first, /events (no param) defaults to newest-first</done>
</task>

</tasks>

<verification>
Test URLs in browser:
- /events?sort=newest - events ordered newest to oldest
- /events?sort=oldest - events ordered oldest to newest
- /events (no param) - defaults to newest-first
</verification>

<success_criteria>
Controller reads sort query parameter, maps to direction, passes to Event::ordered(), defaults to 'newest'.
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-sorting/04-02-SUMMARY.md`
</output>
