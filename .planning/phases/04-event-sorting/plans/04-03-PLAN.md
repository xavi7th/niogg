---
phase: 04-event-sorting
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - Modules/PublicPage/resources/js/Pages/Components/EventTimeline.svelte
  - Modules/PublicPage/resources/js/Pages/Components/EventsGrid.svelte
autonomous: true

must_haves:
  truths:
    - "Sort toggle control appears on EventTimeline component"
    - "Sort toggle control appears on EventsGrid component"
    - "Clicking 'Newest' link updates URL to ?sort=newest and refreshes data"
    - "Clicking 'Oldest' link updates URL to ?sort=oldest and refreshes data"
    - "Active sort link is visually highlighted (orange color, underlined)"
    - "Sort preference persists when toggling between timeline/grid views"
  artifacts:
    - path: "Modules/PublicPage/resources/js/Pages/Components/EventTimeline.svelte"
      provides: "Sort toggle UI for timeline view"
      contains: "$page.url.searchParams.get('sort')"
      min_lines: 30
    - path: "Modules/PublicPage/resources/js/Pages/Components/EventsGrid.svelte"
      provides: "Sort toggle UI for grid view"
      contains: "$page.url.searchParams.get('sort')"
      min_lines: 30
  key_links:
    - from: "EventTimeline.svelte"
      to: "EventsMediaShowcaseController.php"
      via: "Inertia Link with ?sort= query parameter"
      pattern: "href=.*sort="
    - from: "EventsGrid.svelte"
      to: "EventsMediaShowcaseController.php"
      via: "Inertia Link with ?sort= query parameter"
      pattern: "href=.*sort="
---

<objective>
Add sort toggle controls to EventTimeline and EventsGrid components using Inertia Link with URL query parameters.

Purpose: Frontend UI for sort control - users can toggle between newest-first and oldest-first with shareable URLs.
Output: Sort toggle controls on both timeline and grid views with active state styling.
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-sorting/04-CONTEXT.md
@.planning/phases/04-event-sorting/04-RESEARCH.md

# Current implementation
@Modules/PublicPage/resources/js/Pages/Components/EventTimeline.svelte
@Modules/PublicPage/resources/js/Pages/Components/EventsGrid.svelte
@.planning/phases/04-event-sorting/plans/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add sort toggle control to EventTimeline component</name>
  <files>Modules/PublicPage/resources/js/Pages/Components/EventTimeline.svelte</files>
  <action>
    1. Import Link component from Inertia (add to existing imports if needed):
       ```javascript
       import { Link } from '@inertiajs/svelte';
       ```

    2. Add reactive statement to read current sort from URL (after existing reactive statements):
       ```javascript
       $: currentSort = $page.url.searchParams.get('sort') || 'newest';
       ```

    3. Add sort toggle HTML at the TOP of component (inside .event-timeline div, before first event-section):
       ```svelte
       <div class="sort-controls">
         <span class="sort-label">Sort events:</span>
         <Link href="?sort=newest" class:active={currentSort === 'newest'} aria-current={currentSort === 'newest' ? 'true' : undefined}>
           Newest
         </Link>
         <span class="sort-divider">/</span>
         <Link href="?sort=oldest" class:active={currentSort === 'oldest'} aria-current={currentSort === 'oldest' ? 'true' : undefined}>
           Oldest
         </Link>
       </div>
       ```

    4. Add CSS for sort controls (inside existing <style> block):
       ```css
       .sort-controls {
         display: flex;
         align-items: center;
         gap: 0.75rem;
         margin-bottom: 2rem;
         padding: 0 1rem;
       }

       .sort-label {
         color: #666;
         font-size: 0.875rem;
         font-weight: 500;
       }

       .sort-controls a {
         color: #666;
         text-decoration: none;
         font-weight: 600;
         transition: color 0.2s ease;
       }

       .sort-controls a:hover {
         color: #ff7607;
       }

       .sort-controls a.active {
         color: #ff7607;
         text-decoration: underline;
       }

       .sort-divider {
         color: #ccc;
       }
       ```

    DO NOT:
    - Modify the existing event rendering logic
    - Change the video selection behavior
    - Add localStorage or other client-side persistence
    - Use router.get() instead of Link component
  </action>
  <verify>Visit page, click sort links, verify URL updates and events reorder</verify>
  <done>Clicking "Newest" link sets ?sort=newest, clicking "Oldest" link sets ?sort=oldest, active link is orange/underlined, URL updates correctly</done>
</task>

<task type="auto">
  <name>Add sort toggle control to EventsGrid component</name>
  <files>Modules/PublicPage/resources/js/Pages/Components/EventsGrid.svelte</files>
  <action>
    1. Import Link component from Inertia (add to existing imports if needed):
       ```javascript
       import { Link } from '@inertiajs/svelte';
       ```

    2. Add reactive statement to read current sort from URL (after existing reactive statements):
       ```javascript
       $: currentSort = $page.url.searchParams.get('sort') || 'newest';
       ```

    3. Add sort toggle HTML in the grid-header section (after .btn-back button):
       ```svelte
       <div class="sort-controls">
         <span class="sort-label">Sort events:</span>
         <Link href="?sort=newest" class:active={currentSort === 'newest'} aria-current={currentSort === 'newest' ? 'true' : undefined}>
           Newest
         </Link>
         <span class="sort-divider">/</span>
         <Link href="?sort=oldest" class:active={currentSort === 'oldest'} aria-current={currentSort === 'oldest' ? 'true' : undefined}>
           Oldest
         </Link>
       </div>
       ```

    4. Add CSS for sort controls (inside existing <style> block):
       ```css
       .sort-controls {
         display: flex;
         align-items: center;
         gap: 0.75rem;
         margin-top: 1rem;
       }

       .sort-label {
         color: #666;
         font-size: 0.875rem;
         font-weight: 500;
       }

       .sort-controls a {
         color: #666;
         text-decoration: none;
         font-weight: 600;
         transition: color 0.2s ease;
       }

       .sort-controls a:hover {
         color: #ff7607;
       }

       .sort-controls a.active {
         color: #ff7607;
         text-decoration: underline;
       }

       .sort-divider {
         color: #ccc;
       }
       ```

    DO NOT:
    - Modify the existing grid rendering logic
    - Change the video modal behavior
    - Add localStorage or other client-side persistence
    - Use router.get() instead of Link component
  </action>
  <verify>Visit grid view, click sort links, verify URL updates and events reorder</verify>
  <done>Clicking "Newest" link sets ?sort=newest, clicking "Oldest" link sets ?sort=oldest, active link is orange/underlined, sort persists when switching views</done>
</task>

</tasks>

<verification>
Test in browser:
1. Navigate to events page
2. Click "Oldest" - URL becomes ?sort=oldest, events reorder
3. Click "Newest" - URL becomes ?sort=newest, events reorder
4. Toggle between timeline/grid - sort preference maintained
5. Share URL with ?sort=oldest - recipient sees same sort order
</verification>

<success_criteria>
Both EventTimeline and EventsGrid have sort toggle controls using Inertia Link, active state styling works, URL updates correctly, sort persists across navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-sorting/04-03-SUMMARY.md`
</output>
