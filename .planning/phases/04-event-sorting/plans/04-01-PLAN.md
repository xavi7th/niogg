---
phase: 04-event-sorting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/PublicPage/app/Models/Event.php
autonomous: true

must_haves:
  truths:
    - "Event::ordered() scope accepts direction parameter ('asc'|'desc')"
    - "Default direction is 'desc' (newest-first) when no parameter provided"
    - "Scope orders events by event_date column"
  artifacts:
    - path: "Modules/PublicPage/app/Models/Event.php"
      provides: "Dynamic event sorting scope"
      contains: "scopeOrdered"
      min_lines: 5
  key_links:
    - from: "Modules/PublicPage/app/Http/Controllers/EventsMediaShowcaseController.php"
      to: "Modules/PublicPage/app/Models/Event.php"
      via: "Event::ordered(\$direction)"
      pattern: "ordered\\(\\\$?\\w+\\)"
---

<objective>
Modify Event model's ordered() scope to accept a direction parameter, enabling dynamic sort order (newest-first or oldest-first).

Purpose: Backend foundation for URL-based sorting - controller can now pass sort direction from query parameter to model.
Output: Flexible Event::ordered($direction) scope with 'desc' default.
</objective>

<execution_context>
@/Users/leinad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leinad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-sorting/04-CONTEXT.md
@.planning/phases/04-event-sorting/04-RESEARCH.md

# Current implementation
@Modules/PublicPage/app/Models/Event.php
</context>

<tasks>

<task type="auto">
  <name>Modify Event::ordered() scope to accept direction parameter</name>
  <files>Modules/PublicPage/app/Models/Event.php</files>
  <action>
    1. Update scopeOrdered() method signature to accept optional direction parameter:
       - Change: `public function scopeOrdered($query)`
       - To: `public function scopeOrdered($query, ?string $direction = 'desc')`
       - Use nullable string type with default 'desc' to maintain backward compatibility

    2. Update return statement to use the direction parameter:
       - Change: `return $query->orderBy('event_date', 'desc');`
       - To: `return $query->orderBy('event_date', $direction);`

    3. Add PHPDoc comment documenting the parameter:
       ```php
       /**
        * Order events by date
        *
        * @param \Illuminate\Database\Eloquent\Builder $query
        * @param string|null $direction Sort direction: 'asc' (oldest first) or 'desc' (newest first)
        * @return \Illuminate\Database\Eloquent\Builder
        */
       ```

    DO NOT:
    - Change any other scopes or methods
    - Modify the scopePublished() method
    - Add any new methods
    - Change the relationship definitions
  </action>
  <verify>Run: vendor/bin/sail php artisan tinker --execute="Event::ordered('asc')->get(['id', 'event_date']); Event::ordered('desc')->get(['id', 'event_date']);"</verify>
  <done>Calling Event::ordered('asc') returns events ordered by event_date ascending, Event::ordered('desc') returns events ordered by event_date descending, Event::ordered() (no param) defaults to 'desc'</done>
</task>

</tasks>

<verification>
Run tinker test to verify scope behavior:
- Event::ordered('asc') returns events in ascending order
- Event::ordered('desc') returns events in descending order
- Event::ordered() with no parameter defaults to 'desc'
</verification>

<success_criteria>
Event::ordered() scope accepts direction parameter with 'desc' default, enabling controller to dynamically control sort order based on URL query parameter.
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-sorting/04-01-SUMMARY.md`
</output>
